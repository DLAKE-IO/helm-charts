{{- if and .Values.webhookListener.enabled (not .Values.certificates.enabled) (not .Values.webhookListener.tls.existingSecret) }}
{{- fail "webhookListener requires either certificates.enabled=true or webhookListener.tls.existingSecret to be set" }}
{{- end }}
{{- if .Values.webhookListener.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wazuh.fullname" . }}-webhook-script
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "wazuh.fullname" . }}-webhook-listener
data:
  kubernetes-webhook.py: |
    #!/var/ossec/framework/python/bin/python3

    import json
    import os
    from socket import socket, AF_UNIX, SOCK_DGRAM
    from flask import Flask, request

    PORT     = int(os.getenv('WEBHOOK_PORT', '8080'))
    CERT     = os.getenv('WEBHOOK_CERT', '/var/ossec/integrations/kubernetes-webhook/server.crt')
    CERT_KEY = os.getenv('WEBHOOK_CERT_KEY', '/var/ossec/integrations/kubernetes-webhook/server.key')

    socket_addr = '/var/ossec/queue/sockets/queue'

    def send_event(msg):
        string = '1:k8s:{0}'.format(json.dumps(msg))
        sock = socket(AF_UNIX, SOCK_DGRAM)
        sock.connect(socket_addr)
        sock.send(string.encode())
        sock.close()
        return True

    app = Flask(__name__)
    context = (CERT, CERT_KEY)

    @app.route('/', methods=['POST'])
    def webhook():
        if request.method == 'POST':
            if send_event(request.json):
                print("Request sent to Wazuh")
            else:
                print("Failed to send request to Wazuh")
        return "Webhook received!"

    if __name__ == '__main__':
        app.run(host='0.0.0.0', port=PORT, ssl_context=context)
{{- end }}
